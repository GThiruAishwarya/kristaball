# client/.env

# The base URL for your backend API.
# This should match the port your Express server is running on (default 5000).
# If your backend is deployed, you'll change this to the deployed URL.
REACT_APP_API_BASE_URL=http://localhost:5000/api
